server:
  port: 8085

spring:
  application:
    name: ms-progresso
  datasource:
    url: jdbc:postgresql://postgres:5432/ms-progresso
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
