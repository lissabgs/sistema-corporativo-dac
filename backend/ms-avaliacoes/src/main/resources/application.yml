server:
  port: 8084

spring:
  application:
    name: ms-avaliacoes
  datasource:
    url: jdbc:postgresql://postgres:5432/ms-avaliacoes
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
